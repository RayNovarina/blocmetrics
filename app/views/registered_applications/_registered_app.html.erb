<div class="row css-regd-app-container" style="background: white;">

  <div class="col-md-8">
    <div class="media">
      <div class="media-body">
        <h4 class="media-heading">
            <span class="css-regd-app-name" style="overflow: hidden; text-decoration: underline;">
              <%= link_to @view.locals[:app].name,
                          registered_application_path(@view.locals[:app]),
                          title: "Created by: #{@view.locals[:app].user.name}" %>
            <span>
            <span style="color: black; font-weight: bold; ext-decoration: none;">&nbsp;at&nbsp;</span>
            <span class="css-regd-app-url" style="overflow: hidden; text-decoration: underline;">
              <%= link_to @view.locals[:app].url, @view.locals[:app].url, target: '_blank' %>
            <span>
        </h4>
      </div>
    </div>
  </div>

  <div class="col-md-4">
    <div class="media">
      <div class="media-body">
        <h4 class="media-heading">
          <% if true #policy(props[:bookmark]).destroy? && rpt_bmk(props)[:include_delete] %>
            <span class="pull-right css-regd-app-action" style="padding-top: 4px; padding-right: 4px;">
              <%= link_to 'Remove', @view.locals[:app], method: :delete,
                          class: 'btn btn-xs btn-danger',
                          data: {confirm: 'Are you sure you want to delete this app?'} %>
            </span>
          <% end %>
          <% if true #policy(props[:bookmark]).edit? && rpt_bmk(props)[:include_edit] %>
            <span class="pull-right css-regd-app-action" style="padding-top: 4px; padding-right: 4px;">
              <%= link_to 'Edit', edit_registered_application_path(@view.locals[:app]),
                          class: 'btn btn-xs btn-success' %>
            </span>
          <% end %>
        </h4>
      </div>
    </div>
  </div>

</div>
